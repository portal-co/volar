[package]
name = "volar-macros"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Proc macros for Volar generated code — action attributes, storage hooks"
repository.workspace = true

[lib]
proc-macro = true

[dependencies]
